@model IEnumerable<RealEstatePro.Models.Advertisement>

@{
    ViewBag.Title = "StatusList";
}

<div class="container">

    <br />
    <div class="row">
        <!--slider-->
        <div class="col-md-12">
            <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">

                <!-- Wrapper for slides -->

                <div class="carousel-inner" role="listbox">
                    @Html.Action("Slider", "Home")
                </div>

                <!-- Controls -->
                <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
                    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
                    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>

    </div>


    <div class="row">
        <!--Filtreleme-->
        @Html.Action("PartialFilter", "Home")

        <!--ilan kısım-->

        <br />

        @foreach (var item in Model)
        {
            var c = 1;
            <div class="col-sm-6 col-md-3">
                <div class="thumbnail">
                    @foreach (var rsm in ViewBag.imgs)
                    {
                        if (rsm.AdvId == item.AdvId)
                        {
                            if (@c == 1)
                            {
                                <a href="/Home/Details/@item.AdvId">  <img src="~/Content/images/@rsm.AdvPhotoName" alt="..." style="height:300px;"></a>
                                c++;
                            }

                        }

                    }

                    <div class="caption">
                        <p><strong>@item.Type.Status.StatusName</strong> (@item.Type.TypeName) </p>
                        <p>@item.Neighborhood.District.City.CityName  - @item.Neighborhood.District.DistrictName</p>
                        <p>
                            <button class="btn btn-danger"><i class="fa fa-usd" aria-hidden="true"></i> @item.Price </button>
                            <button class="btn btn-primary">@item.Area m<sup>2</sup></button>
                            <button class="btn btn-success pull-right"><i class="fa fa-bed" aria-hidden="true"></i> @item.NumOfRoom</button>

                        </p>
                        <div style="align-items: center; display: flex;justify-content: center;">
                            <button class="btn btn-success btn-block" style="border: 3px solid green;" onclick="location.href='/Home/Details/@item.AdvId'" type="button">
                                For More details click here
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        }




    </div>

</div>

